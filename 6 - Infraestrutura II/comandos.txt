ansible-playbook playbook.yaml -i inventory


sudo su -   (ou)   sudo -i  (para entrar como usuário autorizado)

docker system info

docker ps -a (para verificar os containers rodando)

docker rm -f codigo do container, container id (usado para apagar o container) 

docker run -d -p 81:80 httpd
docker run -d -p 80:80 nginx

meu ip público naquela máquina
curl ifconfig.me